This folder contains the reviewer-facing figure for PR1.

Asset:
- `pr1_geometry_workflow.png`: package-backed workflow overview and package-content snapshot for the stellarator geometry foundation layer.

Suggested caption:

```text
PR1 introduces a package-backed geometry workflow that preserves the current stellarator branch behavior while making geometry easier to cache, reload, and test.
```

Suggested insertion point:
- after the opening summary and before the "What this PR adds" section
